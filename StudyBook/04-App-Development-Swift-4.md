Swift でのアプリ開発 4
今回で Apple Books での学習は最後です。
この学習を終えると iOS アプリ開発の基礎を一通り学んだことになります。

今回の環境は、

macOS Big Sur 11.4
Xcode 12.5.1 (12E507)
です。

目次
Table View
アクションとアウトレット
アウトレット (Outlet)
アクション (Action)
接続状態
接続の削除と再接続
列挙型と switch 文
Table View
何らかのリストを扱うアプリでは、 Table View が使われることが多いです。

iOS に標準でインストールされている、設定やミュージック、連絡先なども Table View を使用しています。

Table View を実装するには UIKit の UITableView クラスに関係するクラスやプロトコルを使用します。

また、 Swift でリストを表現するには配列が適していることを学習しました。
配列と Table View を使ってユーザにリストの一覧が見られるようにしてみましょう。

まず、 Table View には セル (Cell) が載っています。
このセルはリストのそれぞれの要素を表示するための View です。

基本的に Table View は View Controller に配置されるので、 View の階層構造は以下のようになります。

View Controller の View > Table View > Cell

この Cell と、表示したい要素をもつ配列を対応させるなど、 Table View の設定を行うには データソース (DataSource) を使用します。
また、 Table View の操作に対するイベントを受け取るには デリゲート (Delegate) を使用します。

具体的には、 UITableViewDataSource と UITableViewDelegate です。

UITableViewDataSource で必ず実装しなければいけないメソッドが以下です。

tableView(_:cellForRowAt:)
tableView(_:numberOfRowsInSection:)
tableView(_:cellForRowAt:)
これはある IndexPath に対する Cell を定義するメソッドです。

IndexPath とは Table View の中で位置を表すもので、プロパティに section と row があります。
section は文字通りセクションを、 row は行を表します。

tableView(_:numberOfRowsInSection:)
これはあるセクションにいくつの要素があるかを示すメソッドです。

UITableViewDataSource を実装する例は以下のようなものがあります。

class TableViewController: UIViewController {
  let list = ["a", "b", "c", "d", "e"]
  @IBOutlet weak var tableView: UITableView!

  var listCount: Int {
    return list.count
  }

  override func viewDidLoad() {
    tableView.dataSource = self
  }
}

extension TableViewController: UITableViewDataSource {
  func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    let cell = UITableViewCell()
    cell.titleLabel.text = list[indexPath.row]
    return cell
  }

  func numberOfSections(in tableView: UITableView) -> Int {
    return listCount
  }
}

すでに 1 章でこのようなコードを書いたと思いますが、今一度復習してみましょう。

TableViewController のプロパティには Table View に表示させたい要素である list と Table View である tableView 、そして list の数を返す listCount があります。

tableView には IBOutlet attribute がついているので Storyboard と接続されている想定です。

listCount は計算プロパティです。
これは getter や setter を提供するもので、このプロパティが参照された時/値が代入された時にどのような処理を行うかを記述できます。
今回は getter のみの計算プロパティで、参照された時に list.count を返すようにしています。

なお、今まで扱ってきた list や tableView のような値を保持しているだけのプロパティを格納プロパティと呼びます。

まず、 viewDidLoad にて、 tableView のデータソースを指定しています。
これにより TableViewController クラスで実装した UITableViewDataSource のメソッドが発火されるようになります。

そして extension として UITableViewDataSource を実装しています。

アクションとアウトレット
ビューはアプリの UI の構成要素です。
すべてのビューは UIView クラス、あるいは UIView のサブクラスのインスタンスです。

また、ユーザが操作可能なビューはコントロールと呼ばれ、UIControl クラスを継承して実装されています。
代表的なものに、ボタン (UIButton) やスイッチ (UISwitch) などがあります。

ビューを GUI で構築するために Storyboard を使用します。
静的な画面だと Storyboard で完結しますが、ある画面で要素の表示を変えたり、ユーザからのイベントに反応したりすることが多いです。

例えば、次のような動作です。

画面を表示したら通信を行い、得られた情報を画面のラベルに表示する
ユーザがボタンをタップしたらラベルのテキストを変更する
このような動的な画面は Storyboard をソースコードと紐付けて開発を行います。

アウトレット (Outlet)
アウトレット接続をすると、ビューのプロパティやメソッドにアクセスできます。
ここでは例として、ラベルのテキストや外観をコードから設定してみます。

Storyboard に Label オブジェクト (UILabel) を配置します。
アシスタントエディタを開いて、Storyboard とコードを分割表示します。
Label を選択して control ⌃ を押したまま Storyboard から コードにドラッグ & ドロップします。
ポップアップメニューの Connection を Outlet に、Name を label に設定して connect をクリックします。
キーボードショートカットを確認する

アウトレット接続
label という名前の UILabel 型のプロパティが作成されました。
@IBOutlet は、このプロパティがアウトレットであることを表します。

@IBOutlet weak var label: UILabel!

ラベルのテキストを変更してみましょう。
以下のように text プロパティに表示させたい文字列を代入します。

label.text = "Outlet 接続"

続いて、テキストの水平方向の配置を設定してみましょう。
textAlignment プロパティを使用します。

ここでは NSTextAlignment.center を代入して中央寄せにしました。

label.textAlignment = .center

ここまでのコードは以下のようになっています。

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var label: UILabel!

    override func viewDidLoad() {
        super.viewDidLoad()

        label.text = "Outlet 接続"
        label.textAlignment = .center
    }

}
Please Type!
アクション (Action)
コントロールとメソッドをアクション接続することで、ユーザの操作に応じてメソッドを実行できます。
ここでは例として、スイッチを切り替えたときにラベルのテキストやスイッチの外観を変更するようにしてみます。

Storyboard に Switch オブジェクト (UISwitch) を配置します。
アシスタントエディタを開いて、Storyboard とコードを分割表示します。
Switch を選択して control ⌃ を押したまま Storyboard から コードにドラッグ & ドロップします。
ポップアップメニューの Connection を Action に、Name を switchChanged に、Type を UISwitch に設定して connect をクリックします。
なお、ポップアップメニューの Event では、対象とするイベントの種類を指定できます。
ここではスイッチが切り替えられたときに発生する Value Changed (valueChanged) イベントを指定しています。


アクション接続
switchChanged という名前のメソッドが作成されました。
@IBAction 属性は、これがアクションのメソッドであることを表します。

@IBAction func switchChanged(_ sender: UISwitch) {
}

sender 引数は、イベントが発生したオブジェクトへの参照です。
これは先ほどポップアップメニューの Type で設定した型になります。

sender では分かりづらいので、ここでは toggleSwitch に変更します。

@IBAction func switchChanged(_ toggleSwitch: UISwitch) {
}

続いて、スイッチの切り替えに応じてラベルのテキストを変更してみましょう。
オン・オフの状態は isOn プロパティの真偽値で判定できます。

@IBAction func switchChanged(_ toggleSwitch: UISwitch) {
    if toggleSwitch.isOn {
        label.text = "スイッチ ON"
    } else {
        label.text = "スイッチ OFF"
    }
}

スイッチ ON
スイッチ OFF
さらに、スイッチの切り替えに応じて外観も変えてみましょう。
onTintColor プロパティではオンの場合の色を、thumbTintColor プロパティではつまみの色を設定できます。

@IBAction func switchChanged(_ toggleSwitch: UISwitch) {
    if toggleSwitch.isOn {
        label.text = "スイッチ ON"
        toggleSwitch.onTintColor = .black
        toggleSwitch.thumbTintColor = .white
    } else {
        label.text = "スイッチ OFF"
        toggleSwitch.thumbTintColor = .black
    }
}

スイッチ ON
スイッチ OFF
ここまでのコードは以下のようになっています。

import UIKit

class ViewController: UIViewController {

    @IBOutlet weak var label: UILabel!

    override func viewDidLoad() {
        super.viewDidLoad()

        label.text = "Outlet 接続"
        label.textAlignment = .center
    }

    @IBAction func switchChanged(_ toggleSwitch: UISwitch) {
        if toggleSwitch.isOn {
            label.text = "スイッチ ON"
            toggleSwitch.onTintColor = .black
            toggleSwitch.thumbTintColor = .white
        } else {
            label.text = "スイッチ OFF"
            toggleSwitch.thumbTintColor = .black
        }
    }

}
Please Type!
接続状態
1 つのビューは、複数のアクションやアウトレットに接続することができます。

アウトレット、アクションに共通することはコードの左側に連携をするためのアイコンが表示されるようになるということです。
コードを修正した後はこれらのアウトレット、アクションが接続されていることを確認してください。

接続されている（塗りつぶされたアイコン）
接続されていない（白いアイコン）
アウトレットの型は ImplicitlyUnwrappedOptional 型です。
この型は nil かどうかをチェックしないため、参照された際に nil だとアプリはクラッシュします。
アウトレットが接続されていない場合はプロパティが nil のままになってしまいます。

また、アクションは接続されていないとそのメソッドは呼ばれません。
期待通りの動作にならないため、接続を確認しておきましょう。

接続の削除と再接続
次の手順で Storyboard とコードの接続を削除できます。

Storyboard で接続を削除したいオブジェクトを選択します。
Connections inspector を開き、削除したい接続項目の × をクリックします。
ちなみに control ⌃ を押しながら接続を削除したいオブジェクトをクリックすると、Utility Area を開くことなく簡単に操作できます。


接続を削除する
次の手順で Storyboard とコードを再度接続できます。

接続したいアウトレットプロパティ、またはアクションメソッドの左側のアイコンをクリックします。
Storyboard の接続したいオブジェクトにドラッグします。
コードの左側のアイコンが塗りつぶされて、Connections inspector で接続されていることが確認できます。


再度接続する
練習問題
問1
アウトレット
このスクリーンショットの各部の説明として間違っているものは次のうちどれでしょう。

○
一番左の白丸はこの行のコードにエラーがあることを示している。
×
@IBOutlet weak はアウトレットのプロパティであることを表します。

×
var test はプロパティを宣言している。

解説
答えを隠す

問1
一番左の白丸はアウトレットが接続されていないことを示していて、連携をするためのマークでもあります。
そのため、エラーがあることを示している訳ではないので、1 は誤りです。

まとめ
アプリの UI を Interface Builder で作成する際に、動的な要素はアウトレットやアクションを使ってコードと繋げます。

列挙型と Switch 文
列挙型は限られた選択肢を表現するのに最適です。
このセクションでは、列挙型の定義と使用方法、そして列挙型と相性が良い Switch 文の使用方法を学びます。

列挙型
列挙型とは、関連する選択肢のグループを定義するための型です。
例えば、天気や曜日など、限られた選択肢から 1 つを選ぶことのできる値を表現するのに適しています。

まずは基本的な列挙型を定義してみましょう。
曜日を表す列挙型を作成します。

enum DayOfWeek {
    case monday    // 月曜日
    case tuesday   // 火曜日
    case wednesday // 水曜日
    case thursday  // 木曜日
    case friday    // 金曜日
    case saturday  // 土曜日
    case sunday    // 日曜日
}

列挙型は enum[1] キーワードを使用して定義します。
列挙型の識別子は UpperCamelCase にするのが慣習です。

列挙型の各選択肢は case キーワードを使用して定義します。
この選択肢は「列挙ケース」と呼ばれます。
ケースの識別子は単数形にします。

複数のケースを , で区切って 1 行で定義することもできます。

enum DayOfWeek {
    case monday, tuesday, wednesday, thursday, friday // 平日
    case saturday, sunday // 休日
}

以上のように列挙型を定義すると、以下のように使用することができます。

// インスタンスを取得する
let day = DayOfWeek.monday

列挙型と型推論
Swift は型推論が非常に強力な言語です。
以下のように、型が明確な場合は型を省略することができます。

var direction: CompassPoint
direction = .north

Associated Values[2]
ケースに任意の型の値を関連付けることもできます。
これにより、ケースに追加情報を格納して、より複雑なデータも表現することができます。

以下は天気を表す列挙型です。
snowy と rainy のケースに Int 型の値を関連付けています。
雪の場合は積雪量、雨の場合は降水確率の情報を持たせることができます。

enum Weather {
    case sunny      // 晴れ
    case cloudy     // 曇り
    case snowy(Int) // 雪（積雪量）
    case rainy(Int) // 雨（降水確率）
}

let weather = Weather.snowy(30)

// weather が snowy の場合、関連値が snowDepth に束縛される
if case let .snowy(snowDepth) = weather {
    print("積雪量は \(snowDepth) cm です。") // --> "積雪量は 30 cm です。"
}

Raw Values
ケースに固定された値を関連付けることもできます。

Raw Value の型は文字、文字列、整数、浮動小数点数のいずれかである必要があります。
また、すべてのケースが同じ型の値を持ち、それぞれの値は一意である必要があります。

enum EscapeSequence: Character {
    case tab = "\t"
    case lineFeed = "\n"
    case carriageReturn = "\r"
}

Raw Value は rawValue プロパティで取得できます。

let tab = EscapeSequence.tab
tab.rawValue // --> "\t"

また、init(rawValue:) を使用して、指定した Raw Value からインスタンスを作成することもできます。
init(rawValue:) は失敗可能イニシャライザであり、Raw Value が存在しない場合は nil が返ります。

let lineFeed = EscapeSequence(rawValue: "\n")

なお、Raw Value が整数や文字列の場合、明示的に割り当てなくても自動的に割り当てられます。

整数の場合、最初のケースを指定しなければ 0 から始まる連番の Raw Value になります。
つまり、前のケースの数値をインクリメントした数値が割り当てられていきます。

// ここでは ace に 1 を指定しているため、1 から始まる連番が割り当てられる

enum Rank: Int {
    case ace = 1, two, three, four, five, six, seven, eight, nine, ten
    case jack, queen, king
}

Rank.ace.rawValue   // --> 1
Rank.two.rawValue   // --> 2
Rank.three.rawValue // --> 3
// …
Rank.jack.rawValue  // --> 11
Rank.queen.rawValue // --> 12
Rank.king.rawValue  // --> 13

文字列の場合、ケースの名前が Raw Value になります。

enum CompassPoint: String {
    case north, south, east, west
}

CompassPoint.north.rawValue // --> "north"
CompassPoint.south.rawValue // --> "south"
CompassPoint.east.rawValue  // --> "east"
CompassPoint.west.rawValue  // --> "west"

列挙型のプロパティとメソッド
前節のインスタンス、メソッド、プロパティで説明しましたが、列挙型はプロパティやメソッドを持つことができます。
ただし、列挙型は格納プロパティを持つことはできません。

enum CompassPoint: String {
    case north, south, east, west

    var description: String {
        // self はインスタンス自身を表す
        return "方角は \(self.rawValue) です。"
    }

    // Self は型自身を表す
    func turnRight() -> Self {
        return switch self {
            case .north: .east
            case .south: .west
            case .east: .south
            case .west: .north
        }
    }
}

let north = CompassPoint.north
north.description // --> "方角は north です。"

let east = north.turnRight()
east.description // --> "方角は east です。"

if 文の問題点
プログラムでは、さまざまな条件に応じて異なる動作をする必要があります。
これを実現するために、これまで if 文を使用して条件分岐をしてきました。

しかし if 文の問題点として、条件が多くなった場合に分岐が複雑になることが挙げられます。
このような場合は switch 文を使用すると分岐をシンプルに記述することができます。

switch 文
switch 文は 1 つの対象を複数のパターンと比較して、マッチしたパターンの処理を実行します。

switch 文は列挙型と非常に相性が良いです。
列挙型は限定された選択肢を持ち、それぞれのケースを明確にすることができるため、switch 文と組み合わせることで、簡潔で安全なコードを書くことができます。

以下は switch 文の基本的な使用方法です。

let direction = CompassPoint.north

switch direction {
case .north:
    print("北を向いています。") // --> "北を向いています。"
case .south:
    print("南を向いています。")
case .east:
    print("東を向いています。")
case .west:
    print("西を向いています。")
default:
    print("方角が不明です。")
}

以上の switch 文は CompassPoint 型のケースを網羅しているため default ケースが実行されることはありません。
この場合は default ケースを省略することもできます。

Swift の switch 文では、自動的にフォールスルー[3]しないため break 文は不要です。
フォールスルーが必要な場合は fallthrough キーワードで明示的に指定する必要があります。

let direction = CompassPoint.north

switch direction {
case .north:
    print("北を向いています。") // --> "北を向いています。"
    fallthrough
case .south:
    print("南を向いています。") // --> "南を向いています。"
    fallthrough
case .east:
    print("東を向いています。") // --> "東を向いています。"
case .west:
    print("西を向いています。")
default:
    print("方角が不明です。")
}

可能性を網羅する
switch 文では、すべての可能性を網羅する必要があります。
これにより、必然的に条件分岐の漏れがなくなり、安全なコードを書くことができます。

しかし、すべてのケースを具体的に挙げることができない場合もあります。
そのような場合は default ケースを使用して、それ以外のケースを処理することができます。

let mimeType = "image/webp"

switch mimeType {
    case "image/gif": print("GIF 画像です。")
    case "image/png": print("PNG 画像です。")
    case "image/jpg": print("JPG 画像です。")
    default: print("未対応の形式です。") // --> "未対応の形式です。"
}

複数のケース
case の条件として、複数の条件式を , で区切って記述できます。
これを複合ケースと呼びます。

let mimeType = "image/jpg"

switch mimeType {
    case "image/gif": print("GIF 画像です。")
    case "image/png": print("PNG 画像です。")
    case "image/jpg", "image/jpeg": print("JPG 画像です。") // --> "JPG 画像です。"
    default: print("未対応の形式です。")
}

JPG 画像の拡張子は .jpg と .jpeg の 2 種類が一般的に使用されます。

列挙型の関連値を扱う
列挙型のケースが関連値を持つ場合、switch 文のケースでマッチした値を一時的に定数や変数に束縛することができます。
その定数や変数はマッチしたケースのブロックで使用することができます。
この挙動は「値バインディング」と呼ばれます。

また where 句を含めることで、さらに条件を追加することができます。

enum Weather {
    case sunny
    case cloudy
    case snowy(Int) // 雪（積雪量）
    case rainy(Int) // 雨（降水確率）
}

let todayWeather = Weather.rainy(90) // 降水確率 90%

switch todayWeather {
case .sunny:
    print("今日は晴れです。")
case .cloudy:
    print("今日は曇りです。")
case let .snowy(snowDepth):
    print("今日は雪です。積雪量は \(snowDepth)cm です。")
case let .rainy(probabilityOfPrecipitation) where probabilityOfPrecipitation >= 80: // 雨で降水確率が 80% 以上の場合にマッチする
    print("今日は雨です。降水確率は \(probabilityOfPrecipitation)% です。") // --> "今日は雨です。降水確率は 90% です。"
}

if 式と switch 式
Swift では、if と switch は文として使用されますが、Swift 5.9 以降、これらも式として使用できるようになりました。
if 式と switch 式 は結果を返すことができます。

ここでは if 式を例に説明しますが、switch 式も同様のことができます。

例えば、これまでは if 文を使用して、以下のように記述することがありました。
someValue の値を条件に応じて決定したい場合、someValue に再代入する必要があります。

var someValue: Int

if someCondition1 {
    someValue = 1
} else if someCondition2 {
    someValue = 2
} else if someCondition3 {
    someValue = 3
} else {
    someValue = 4
}

新しい if 式を使用すると、以下のように簡潔に記述できます。
someValue に再代入する必要がなくなり、安全で可読性の高いコードになりました。

let someValue = if someCondition1 {
    1
} else if someCondition2 {
    2
} else if someCondition3 {
    3
} else {
    4
}

ただし、どのような場面でも使用できるわけではなく、いくつかの制約があります。
しかし、if 式と switch 式は導入初期段階であり、この制約は将来的に緩和される可能性があります。

if 式と switch 式を使用できるコンテキストは限られています。
具体的には、束縛、代入、スロー、リターンで使用できます。

let someCondition = true

// 束縛
let someConstant = if someCondition { 1 } else { 2 }             // OK

// 代入
var someVariable: Int
someVariable = if someCondition { 1 } else { 2 }                 // OK

// スロー
struct SomeError: Error {}
struct SomeOtherError: Error {}
throw if someCondition { SomeError() } else { SomeOtherError() } // OK

// リターン
func someFunction() -> Int {
    return if someCondition { 1 } else { 2 }                     // OK
}

すべての分岐が同じ型の値を常に返す必要があります。
もし分岐の条件が真であれば、その分岐の値が if 式や switch 式の値となります。
なお、それぞれの分岐は独立して型がチェックされるため、オプショナル型のような複数の型に適用可能な nil を使用する場合、Swift は型を自動推論できず、型注釈や型キャストが必要となります。

let someValue = if someCondition { 1 } else { 2 }           // OK
let someValue: Int? = if someCondition { 1 } else { nil }   // OK
let someValue = if someCondition { 1 } else { nil as Int? } // OK

let someValue = if someCondition { 1 }                      // NG
let someValue = if someCondition { 1 } else { 2.0 }         // NG
let someValue = if someCondition { 1 } else { nil }         // NG

例外をスローする場合を除いて、分岐は複数の式を含むことはできず、単一の式でなければなりません。
また、if 式や switch 式で break, continue, return を使用することはできません。

let someValue = if someCondition { print("1"); 1 } else { print("2"); 2 } // NG

複数の式を含むことができるのは、最後に例外をスローする分岐のみです。
fatalError(_:file:line:) のような返値のない関数を呼び出すこともできます。

let someValue = if someCondition { 1 } else { 2; throw SomeError() }      // OK

Swift には三項演算子もありますが、可読性などの問題で敬遠されることがありました。
if 式と switch 式はコードの可読性と柔軟性を向上させるものと期待されています。

演習問題
列挙型を定義する
以下のモノを表す列挙型を定義してください。

信号機
緑信号 (green)
黄信号 (yellow)
赤信号 (red)
ジグソーパズルのピース
角のピース (corner)
辺のピース (edge)
中央のピース (middle)
音楽アプリケーションの再生モード
標準 (standard)
1 曲リピート (playRepeat)
全曲リピート (repeatAll)
シャッフル (shuffle)
Bool を置換する
以下はゲームの敵キャラクタを表す構造体です。
現在は敵キャラクタが武器を持っているかどうかを Bool 型の weapon プロパティで表しています。
しかし、敵キャラクタが武器を持っているかどうかだけでなく、武器の種類も表現したいと思いました。
Bool 型の weapon プロパティを WeaponType 型の weapon プロパティに置き換えてください。

WeaponType 型は武器を表す列挙型として定義してください。
武器を持っていない (none), 剣を持っている (sword), ゴムハンマーを持っている (rubberMallet) というケースを持つようにしてください。

struct Enemy {
   let strength: Int
   let speed: Int
   let weapon: Bool
}

Switch 文を使用した条件分岐
以下は季節を表す列挙型です。
switch 文を使用して、季節に応じたメッセージを出力する関数を実装してください。
例えば summer の場合は 夏休みは涼しい部屋でプログラミングを学ぼう のようなメッセージです。

enum Season {
    case spring // 春
    case summer // 夏
    case autumn // 秋
    case winter // 冬
}

func printMessage(for season: Season) {
    // TODO: switch 文を使用して、季節に応じたメッセージを出力する
}

printMessage(for: .spring)
printMessage(for: .summer)
printMessage(for: .autumn)
printMessage(for: .winter)

答えを隠す

列挙型を定義する
// 信号機
enum TrafficLight {
    case green, yellow, red
}

//ジグソーパズルのピース
enum PuzzlePiece {
    case corner, edge, middle
}

//音楽アプリケーションの再生モード
enum PlaybackMode {
    case standard, playRepeat, repeatAll, shuffle
}

Bool を置換する
enum WeaponType {
    case none, sword, rubberMallet
}

struct Enemy {
   let strength: Int
   let speed: Int
   let weapon: WeaponType
}

Switch 文を使用した条件分岐
enum Season {
    case spring // 春
    case summer // 夏
    case autumn // 秋
    case winter // 冬
}

func printMessage(for season: Season) {
    switch season {
    case .spring:
        print("春休みだー！🌸")
    case .summer:
        print("夏休みだー！🏄")
    case .autumn:
        print("秋休みだー！🍂")
    case .winter:
        print("冬休みだー！⛄")
    }
}

printMessage(for: .spring) // --> "春休みだー！🌸"
printMessage(for: .summer) // --> "夏休みだー！🏄"
printMessage(for: .autumn) // --> "秋休みだー！🍂"
printMessage(for: .winter) // --> "冬休みだー！⛄"

今まで学習した内容に関する練習問題を解いてみてください。

練習問題
問1
個人情報について列挙型として示せるプロパティとして最も適当なものはどれでしょうか。

×
名前
×
住所
×
両親の名前
○
生まれた曜日
問2
enum Suit: String {
    case spades
    case hearts
    case diamonds
    case clubs
    
    var name: String {
        return self.rawValue
    }
}

この列挙型を宣言した後に次の出力をしないコードは次のうちどれでしょうか。

clubs
diamonds

×
var clubs = Suit.clubs
print(clubs.name)
clubs = .diamonds
print(clubs.name)

○
let clubs = Suit.clubs
print(clubs.name)
clubs = .diamonds
print(clubs.name)

×
var clubs = Suit.clubs
print(clubs.name)
print(Suit.diamonds.name)

×
print(Suit.clubs.name)
print(Suit.diamonds.name)

問3
Xcode で Swift のコードを書くこと場合において enum と switch 文を併用することによるメリットでないのは次のうちどれでしょう。

×
タイプミスに気づきやすくなる。
○
コードが確実に短くなること
×
if 文と異なり、 switch 文は全ての case を網羅しなければならないため必然的に場合分けの漏れがなくなり、安全なコードを書くことができる。
解説
答えを隠す

問1
列挙型にすべきデータは、選択肢があるものです。
制約がほぼなく自由に決めることができるものは列挙型は向きません。

この問題では、両親の名前、名前、住所は制約がほぼなく自由に決めることができます。
生まれた曜日は最大でも7パターンなので列挙型が適しています。

問2
let で宣言しているので clubs の値を変更することができないためこの問題の正解となります。

問3
switch 文と併用しない場合であっても Xcode で列挙型を用いると自動補完が効くためタイプミスが減ります。
enum を用いると確実にそれぞれの変数の定義が長くなります。switch 文を用いることでコードが短く見やすく書くことができる場合もありますが、確実にコード量が減少するとは言い難いでしょう。

まとめ
いくつか場合が限られている時は列挙型を選択するべきです。
switch 文と組み合わせることで場合分けの漏れを防ぎ、安全にプログラミングできます。

これで App Development with Swift での復習は終わりです。
第 1 章と合わせて Swift, iOS 開発の基本がだんだん身についてきたことかと思います。

次からはより実践的な開発手法やツールの使い方を学んでいきます。

挑戦
Apple Books セクション 20 の最終プロジェクトに取り組んでみましょう。

お疲れさまでした！
学習したことをSNSで報告しよう！



enum は enumeration に由来しています。 ↩︎

Associated Values は日本語で「関連値」と訳されることが多いです。 ↩︎

フォールスルーとは、あるケースの処理を実行した後、そのまま次のケースの処理も実行することを指します。 ↩︎



---
## リンク

[UITableViewDataSource](https://developer.apple.com/documentation/uikit/uitableviewdatasource)

[UITableViewDelegate](https://developer.apple.com/documentation/uikit/uitableviewdelegate)

[tableView(_:cellForRowAt:)](https://developer.apple.com/documentation/uikit/uitableviewdatasource/tableview(_:cellforrowat:))

[tableView(_:numberOfRowsInSection:)](https://developer.apple.com/documentation/uikit/uitableviewdatasource/tableview(_:numberofrowsinsection:))

[UIView](https://developer.apple.com/documentation/uikit/uiview)

[UIControl](https://developer.apple.com/documentation/uikit/uicontrol)

[UIButton](https://developer.apple.com/documentation/uikit/uibutton)

[UISwitch](https://developer.apple.com/documentation/uikit/uiswitch)

[UILabel](https://developer.apple.com/documentation/uikit/uilabel/)

[Text(UILabel)](https://developer.apple.com/documentation/uikit/uilabel/text)

[TextAlignment(UILabel)](https://developer.apple.com/documentation/uikit/uilabel/textalignment)

[NSTextAlignment.center](https://developer.apple.com/documentation/uikit/nstextalignment/center)

[valueChanged](https://developer.apple.com/documentation/uikit/uicontrol/event/valuechanged)

[isOn](https://developer.apple.com/documentation/uikit/uiswitch/ison)

[onTintColor](https://developer.apple.com/documentation/uikit/uiswitch/ontintcolor)

[thumbTintColor](https://developer.apple.com/documentation/uikit/uiswitch/thumbtintcolor)

[init(rawValue:)](https://developer.apple.com/documentation/swift/rawrepresentable/init(rawvalue:))
